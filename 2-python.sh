#!/bin/bash

docker run -it --rm --name "devops_class_exo2"  python:3

# -it = mélage entre -i (interactive donc reçoit les entrées clavier) et -t (tty affiche les evenements (affiche le terminal etc.))